This is command line version
Command format
<ProgramName> <Mode> <FileName>

Mode: 'C' for compression
      'E' for Extract

FileName: FIle or folder to be compressed or Extracted

1. For many file in a folder this program may be crashed
2. after compression a folder named C will be create in Same directory
3. After extract a folder named E will be create in same directory
